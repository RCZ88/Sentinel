# Sentinel — Local Threat Scanner (Windows 11)
# Core runtime dependencies
psutil>=5.9
httpx>=0.27
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
PyYAML>=6.0
rich>=13.7
cryptography>=42.0

# Windows-only (installed automatically on Windows; ignored elsewhere)
pywin32>=306 ; platform_system == "Windows"
wmi>=1.5 ; platform_system == "Windows"
